# YT05 Teardown Receipt — 2026-07-07

## Scope

This receipt records the project-scoped teardown of the Save The Wildlife deployment in:

- OCI region: `uk-london-1`
- Compartment: `Wojciech_P`
- Foundation deployment ID: `YT05`
- DevOps deployment ID: `GWFq`

The compartment itself and unrelated OCI resources were intentionally preserved.

## Deleted Resources

- Kubernetes namespace `ingress-nginx` and its public OCI load balancer.
- Terraform DevOps stack: 25 resources.
- Terraform foundation stack: 73 resources, including OKE, six worker nodes, two standalone compute instances, Autonomous Database, Vault, secrets, networking, notifications, and Terraform-managed IAM resources.
- Eight `save-the-wildlife/*` OCIR repositories containing 1,160 images.
- Two live temporary `codex_temp_ocir_*_YT05` IAM policies. Seven older `stwl-temp-*` policy snapshots were already absent from OCI.
- Two Save The Wildlife compute console-history records.

## Verification

- Both Terraform state files contain zero resources.
- The OCI DevOps project returns `404 NotAuthorizedOrNotFound` after deletion.
- OCIR returns no repository whose name starts with `save-the-wildlife/`.
- Root and project-compartment IAM queries return no `YT05`, `stwl*`, or `codex_temp_ocir*` project policy.
- The OKE cluster is `DELETED`.
- Autonomous Database and both standalone compute instances are `TERMINATED`.
- The ingress load balancer and its Kubernetes namespace are absent.

## OCI Retention Semantics

OCI KMS and Vault use scheduled deletion rather than immediate physical deletion. The YT05 vault, key, and five secrets are all `PENDING_DELETION` for `2026-08-06`. This includes the independently created PAF Canvas SSH-key secret, which was added to the same deletion schedule during the teardown.

OCI Resource Search may continue to show historical DevOps build-run and deployment records after their parent project is deleted. The parent project is confirmed absent, so those indexed records are non-operational history rather than live project infrastructure.
