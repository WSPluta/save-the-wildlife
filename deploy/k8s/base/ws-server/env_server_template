PORT=3000
BROADCAST_REFRESH_UPDATE=50

ENABLE_COHERENCE_BACKEND=true
REALTIME_CLUSTER_BACKEND=coherence
WS_SERVER_REPLICAS=4
COHERENCE_SOCKET_BUS_MAP=socketEvents
COHERENCE_SOCKET_BUS_TTL_MS=60000
COHERENCE_SOCKET_EVENT_MAX_PAYLOAD_BYTES=65536
COHERENCE_SCAN_TIMEOUT_MS=6000
COHERENCE_SCAN_BACKOFF_MS=15000

CLEANUP_STALE_IN_SECONDS=2
BROADCAST_ITEMS_IN_SECONDS=2
ITEMS_REFRESH_MS=1000
POWERUP_REFRESH_MS=2000

ITEM_MAX_SIZE=0.9
ITEM_MIN_SIZE=0.5

WORLD_SIZE_X=128
WORLD_SIZE_Z=42

GAME_DURATION_IN_SECONDS=60

# Physics tuning
PHYS_ACCELERATION=6
PHYS_BRAKE=3
PHYS_MAX_SPEED=3
PHYS_FRICTION=1.5
PHYS_TURN_SPEED=0.78
PHYS_DRIFT=0

# Server authority
SERVER_AUTH_ENABLED=true
SIM_TPS=60
STATE_BROADCAST_HZ=20
COLLISION_VALIDATE_RADIUS=3.6
PICKUP_TOUCH_FORGIVENESS=0.2

# Power-ups
POWERUP_SPEED_MULTIPLIER=2
POWERUP_SPEED_DURATION_MS=10000
POWERUP_SHIELD_DURATION_MS=5000
POWERUP_MAGNET_DURATION_MS=8000
POWERUP_FREEZE_DURATION_MS=4000

# Internal Score service (avoid hairpin through Ingress)
SCORE_SERVICE_HOST=score
SCORE_SERVICE_PORT=8080

# Gameplay telemetry and commentator integration
GAME_EVENTS_DB_ENABLED=true
GAME_EVENTS_DB_USER=ADMIN
GAME_EVENTS_DB_PASSWORD=TEMPLATE_ADB_PASSWORD
GAME_EVENTS_DB_CONNECT_STRING=TEMPLATE_ADB_SERVICE_high
GAME_EVENTS_AUTO_INIT=true
GAME_EVENTS_SERVICE_BASE_URL=http://score:8080
PAF_AGENT_BASE_URL=http://private-agent-factory:8080
PAF_AGENT_TIMEOUT_MS=65000

# Disable local replay fallback (use Replay service via ClusterIP/Ingress)
ENABLE_REPLAY_FALLBACK=false
