resources:
  - private-agent-factory.yaml
  - private-agent-factory-svc.yaml

configMapGenerator:
  - name: private-agent-factory-env
    envs:
      - application.env
  - name: private-agent-factory-wallet-zip
    files:
      - wallet.zip
