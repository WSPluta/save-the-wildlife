apiVersion: v1
kind: Service
metadata:
  labels:
    app: private-agent-factory
  name: private-agent-factory
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: private-agent-factory
